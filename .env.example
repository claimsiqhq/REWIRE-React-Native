# REWIRE Application - Environment Variables
# Copy this file to .env and fill in your values

# ============================================
# DATABASE (Required)
# ============================================
# PostgreSQL connection - Replit provides this automatically
# When you create a PostgreSQL database on Replit, these are set automatically
DATABASE_URL=postgresql://user:password@host:5432/database
PGHOST=your-db-host
PGPORT=5432
PGUSER=your-db-user
PGPASSWORD=your-db-password
PGDATABASE=your-db-name

# ============================================
# AUTHENTICATION (Required)
# ============================================
# Session secret for encrypting cookies - use a long random string
SESSION_SECRET=your-super-secret-session-key-at-least-32-chars

# ============================================
# OPENAI API (Required for AI Features)
# ============================================
# Get your API key from https://platform.openai.com/api-keys
# Used for Coach Brian AI chat and voice features
OPENAI_API_KEY=sk-your-openai-api-key

# Replit AI Integrations (auto-populated if using Replit's AI Integration)
AI_INTEGRATIONS_OPENAI_API_KEY=auto-populated-by-replit
AI_INTEGRATIONS_OPENAI_BASE_URL=auto-populated-by-replit

# ============================================
# EMAIL - SENDGRID (Required for Email Features)
# ============================================
# Get your API key from https://sendgrid.com
# Used for welcome emails, coach invites, reminders
SENDGRID_API_KEY=SG.your-sendgrid-api-key

# The "From" email address (must be verified in SendGrid)
SENDGRID_FROM_EMAIL=your-verified-email@domain.com

# ============================================
# REPLIT ENVIRONMENT (Auto-populated)
# ============================================
# These are automatically set by Replit
REPLIT_DOMAINS=auto-populated
REPLIT_DEV_DOMAIN=auto-populated
REPL_ID=auto-populated
